# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: mytest.spec.ts >> амжилттай нэвтрэх
- Location: tests/mytest.spec.ts:3:5

# Error details

```
Error: expect(locator).toContainText(expected) failed

Locator: getByText('Products')
Expected substring: "test failing text"
Received string:    "Products"
Timeout: 5000ms

Call log:
  - Expect "toContainText" getByText('Products') with timeout 5000ms
  - waiting for getByText('Products')
    14 × locator resolved to <span class="title" data-test="title">Products</span>
       - unexpected value "Products"

```

```yaml
- text: Products
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test('амжилттай нэвтрэх', async ({ page }) => {
  4  |     await page.goto('https://www.saucedemo.com');
  5  |     // Login
  6  |     await page.getByPlaceholder('Username').fill('standard_user');
  7  |     await page.getByPlaceholder('Password').fill('secret_sauce');
  8  |     await page.getByRole('button', { name: 'Login' }).click();
  9  | 
  10 |     // Нүүр хуудас дээр очсон, текст нь харагдаж байгаа эсэхийг шалгах
> 11 |     await expect(page.getByText('Products')).toContainText('test failing text');
     |                                              ^ Error: expect(locator).toContainText(expected) failed
  12 |     await expect(page).toHaveURL('https://www.saucedemo.com/inventory.html');
  13 | 
  14 |     // Logout
  15 |     await page.getByRole('button', { name: 'Open Menu' }).click();
  16 |     await page.getByRole('link', { name: 'Logout' }).click();
  17 |     await expect(page).toHaveURL('https://www.saucedemo.com/');
  18 | });
  19 | 
  20 | test('амжилтгүй нэвтрэх', async ({ page }) => {
  21 |     await page.goto('https://www.saucedemo.com');
  22 |     // Login
  23 |     await page.getByPlaceholder('Username').fill('standard_user');
  24 |     await page.getByPlaceholder('Password').fill('wrong_pass');
  25 |     await page.getByRole('button', { name: 'Login' }).click();
  26 | 
  27 |     // Нүүр хуудас дээр очоогүй байгаа эсэхийг шалгах
  28 |     await expect(page.getByText('Products')).not.toBeVisible();
  29 |     await expect(page).toHaveURL('https://www.saucedemo.com/');
  30 | 
  31 |     // Алдааны мессеж шалгах
  32 |     await expect(page.getByTestId('error')).toContainText(
  33 |         'Username and password do not match',
  34 |     );
  35 | });
  36 | 
  37 | test('бараа сагслах', async ({ page }) => {
  38 |     await page.goto('https://www.saucedemo.com');
  39 |     // Login
  40 |     await page.getByPlaceholder('Username').fill('standard_user');
  41 |     await page.getByPlaceholder('Password').fill('secret_sauce');
  42 |     await page.getByRole('button', { name: 'Login' }).click();
  43 | 
  44 |     // Нүүр хуудас дээр очсон, текст нь харагдаж байгаа эсэхийг шалгах
  45 |     await expect(page.getByText('Products')).toBeVisible();
  46 |     await expect(page).toHaveURL('https://www.saucedemo.com/inventory.html');
  47 | 
  48 |     // Эхний бүтээгдэхүүний "Add to cart" товчийг дарах
  49 |     await page.getByRole('button', { name: 'Add to cart' }).first().click();
  50 |     // "Remove" товч харагдаж байгаа эсэхийг шалгах
  51 |     await expect(page.getByRole('button', { name: 'Remove' }).first()).toBeVisible();
  52 |     // Сагсны тоо зөв эсэх
  53 |     await expect(page.getByTestId('shopping-cart-badge')).toHaveText('1');
  54 | 
  55 |     // Сагс руу орж шалгах
  56 |     await page.getByTestId('shopping-cart-link').click();
  57 |     // Эхний бүтээгдэхүүний нэр харагдаж байгаа эсэхийг шалгах
  58 |     await expect(page.getByText('Sauce Labs Backpack')).toBeVisible();
  59 | 
  60 |     // Logout
  61 |     await page.getByRole('button', { name: 'Open Menu' }).click();
  62 |     await page.getByRole('link', { name: 'Logout' }).click();
  63 |     await expect(page).toHaveURL('https://www.saucedemo.com/');
  64 | });
  65 | 
```